# Versi DIPATOK (==), bukan >=. Alat ini menjanjikan ukuran dalam milimeter, dan
# angka itu keluar dari geometri vtracer/ezdxf/svgpathtools — upgrade diam-diam
# saat rebuild bisa menggeser hasilnya tanpa satu pun cek gagal. Naikkan versinya
# dengan sengaja, lalu jalankan ./check.sh sebelum dipakai produksi.
#
# Yang dipatok di sini cuma dependensi LANGSUNG. Kalau suatu saat ada dependensi
# turunan (mis. scipy lewat svgpathtools) yang menggeser hasil, ganti berkas ini
# dengan `pip freeze` penuh.
Pillow==12.3.0
numpy==2.5.2
opencv-python-headless==5.0.0.93
vtracer==0.6.15
ezdxf==1.4.4
svgpathtools==1.7.2
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32

# Hapus background pada mode Grayscale memakai flood-fill warna (built-in, tanpa model).
# rembg tak lagi diperlukan.
